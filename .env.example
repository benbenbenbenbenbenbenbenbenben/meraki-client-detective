# Meraki Client Detective CLI Configuration
# Copy this file to .env and fill in your values

# Meraki Dashboard API Key
# Get your API key from: https://dashboard.meraki.com/api_access
MERAKI_DASHBOARD_API_KEY=your_api_key_here

# Organization ID (optional - will auto-select if only one org)
# If you have multiple orgs, specify the one to use by default
MERAKI_ORG_ID=

# Network ID (optional - will auto-select if only one wireless network)
# If you have multiple networks, specify the one to use by default
MERAKI_NETWORK_ID=

# Note: If ORG_ID or NETWORK_ID are not set, the CLI will prompt you to select
# from available options